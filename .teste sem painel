deployment:
  tasks:
    - if [ "$BRANCH" = "main" ]; then export DEPLOYPATH=/home1/signae21/public_html; rsync -r * $DEPLOYPATH; fi
    - if [ "$BRANCH" = "miguel" ]; then export DEPLOYPATH=/home1/signae21/miguel_html; rsync -r * $DEPLOYPATH; fi